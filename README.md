# Cue Demo Checkout

A tiny storefront configuration repository used by Cue's Slack decision-to-execution demo.

Cue may draft changes only under `config/` and `docs/`. Every write requires Slack approval and opens a draft pull request.
